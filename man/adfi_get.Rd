% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adfi_get.R
\name{adfi_get}
\alias{adfi_get}
\title{Comprehensive Average Daily Feed Intake (ADFI) Calculation Pipeline}
\usage{
adfi_get(data, adg_res, station_type = NULL, quiet = FALSE)
}
\arguments{
\item{data}{A data.table or data.frame containing feed intake records}

\item{adg_res}{Result list from adg_get() function containing ADG information}

\item{station_type}{Character. Type of feeding station: "nedap" or "fire".
If NULL, will auto-detect from data structure.}

\item{quiet}{Logical. If TRUE, suppresses messages and warnings. Default: FALSE.}
}
\value{
A \code{list} containing:
\itemize{
\item \code{adfi_info}:
\itemize{
\item Aggregated feed intake metrics data.table
\item Contains columns: \code{responder}, \code{location}, \code{origin_dfi}, \code{corrected_dfi}
}
\item \code{adfi_data}:
\itemize{
\item Detailed daily feed intake data.table
\item Contains columns: \code{responder}, \code{location}, \code{date}, \code{seq_days},
\code{origin_dfi}, \code{corrected_dfi}, \code{state}
\item State indicators: "org" (original), "corr" (corrected),
"iii" (interpolated), "elrp" (extrapolated)
}
}
}
\description{
Integrated system for calculating porcine average daily feed intake (ADFI)
through error detection, correction, and imputation. Processes and calibrates
feed intake data from feeding stations.
}
\details{
\strong{Processing Workflow}:
\enumerate{
\item \strong{Error Detection}:
\itemize{
\item Generates error variables based on station type
\item Identifies feed intake anomalies through predefined thresholds
\item Flags problematic records for correction
}

\item \strong{Error Correction}:
\itemize{
\item Calculates error proportions by animal and day
\item Separates valid vs. error-containing intake records
\item Applies LMM modeling for calibration of error feeds
}

\item \strong{ADFI Calculation}:
\itemize{
\item Combines corrected and valid intake records
\item Performs temporal imputation for missing data points
\item Calculates average daily feed intake metrics
}
}

\strong{Supported Station Types}:
\itemize{
\item \strong{NEDAP}: Processes responder, location, date, and state-based data
\item \strong{FIRE}: Processes entrancetime/exittime and weight-based feeding data
}
}
\examples{
result_nedap <- preprocess_data(data = mintyr::nedap, station_type = "nedap", quiet = TRUE)
adg_result <- adg_get(result_nedap, my_break = c(30,120), quiet = TRUE)
adfi_result <- adfi_get(result_nedap, adg_result, quiet = TRUE)
head(adfi_result$adfi_info)
head(adfi_result$adfi_data)
}
\seealso{
\itemize{
\item \code{\link[=adg_get]{adg_get()}} For average daily gain calculation
}
}
